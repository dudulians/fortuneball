import type { FitFont } from "./fitText";

/** The typeface moulded into the die — the same family as the rest of the app, heavier and tracked. */
export const DIE_FONT: FitFont = { family: `"Manrope", "Helvetica Neue", Arial, sans-serif`, weight: 600 };
export const DIE_LETTER_SPACING_EM = 0.08;
